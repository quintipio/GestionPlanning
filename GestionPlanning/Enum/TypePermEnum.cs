namespace GestionPlanning.Enum
{
    /// <summary>
    /// Enum pour les types de permissions posé par l'administrateur sur le calendrier
    /// </summary>
    public enum TypePermEnum
    {
        PCP = 1,
        RTT = 2
    }
}